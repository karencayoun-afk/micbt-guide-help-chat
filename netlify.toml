[build]
  functions = ".netlify/functions"
  publish = "."
  node_bundler = "esbuild"

[build.environment]
  NODE_VERSION = "18"

[functions]
  included_files = ["micbt_faq_database.xml", "faq_vectors.json"]
